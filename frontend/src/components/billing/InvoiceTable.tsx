import type { Invoice } from "./types";
type InvoiceTableProps = { invoices: Invoice[]; search: string; status: string; onSearchChange: (value: string) => void; onStatusChange: (value: string) => void; onCollect: (invoice: Invoice) => void };
export function InvoiceTable({ invoices, search, status, onSearchChange, onStatusChange, onCollect }: InvoiceTableProps) { const filtered = invoices.filter((invoice) => `${invoice.number} ${invoice.patient}`.toLowerCase().includes(search.toLowerCase()) && (!status || invoice.status === status)); return <section className="rounded-xl border border-[#d8e2d9] bg-white p-5"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-sm font-bold uppercase tracking-[.1em] text-[#19b3a2]">Invoices</p><h3 className="mt-1 text-xl font-bold text-[#17362c]">Recent invoices</h3></div><div className="flex flex-col gap-2 sm:flex-row"><input value={search} onChange={(event) => onSearchChange(event.target.value)} placeholder="Search invoice, patient..." aria-label="Search invoice or patient" className="rounded-lg border border-[#c7d5ca] px-3 py-2 text-sm outline-none focus:border-[#19b3a2]" /><select value={status} onChange={(event) => onStatusChange(event.target.value)} aria-label="Filter invoice status" className="rounded-lg border border-[#c7d5ca] px-3 py-2 text-sm"><option value="">All status</option><option>Paid</option><option>Pending</option><option>Overdue</option><option>Cancelled</option></select></div></div><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[680px] text-left text-sm"><thead className="border-b border-[#d8e2d9] text-xs uppercase tracking-[.08em] text-[#587068]"><tr>{["Invoice", "Patient", "Date", "Amount", "Status", "Action"].map((heading) => <th key={heading} className="px-3 py-3 font-semibold">{heading}</th>)}</tr></thead><tbody>{filtered.map((invoice) => <tr key={invoice.id} className="border-b border-[#edf2ef]"><td className="px-3 py-4 font-semibold">{invoice.number}</td><td className="px-3 py-4">{invoice.patient}</td><td className="px-3 py-4">{invoice.date}</td><td className="px-3 py-4 font-semibold">₹{invoice.amount.toLocaleString("en-IN")}</td><td className="px-3 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${invoice.status === "Paid" ? "bg-[#eaf5ef] text-[#0d523e]" : invoice.status === "Overdue" ? "bg-[#fce9eb] text-[#8a1f2d]" : invoice.status === "Cancelled" ? "bg-[#f1f3f2] text-[#587068]" : "bg-[#fff7e9] text-[#8a5e00]"}`}>{invoice.status}</span></td><td className="px-3 py-4"><button type="button" onClick={() => invoice.status === "Pending" || invoice.status === "Overdue" ? onCollect(invoice) : undefined} className="font-semibold text-[#138f82]">{invoice.status === "Pending" || invoice.status === "Overdue" ? "Collect" : "View"}</button></td></tr>)}</tbody></table>{filtered.length === 0 && <p className="py-8 text-center text-sm text-[#587068]">No invoices match your filters.</p>}</div></section>; }